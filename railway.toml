[build]
builder = "nixpacks"

[deploy]
startCommand = "node scripts/seed-data.js && next start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
